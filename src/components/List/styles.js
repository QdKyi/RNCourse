import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        width: '100%',
        borderRadius: 12,
        padding: 10,
        margin: 4,
    },
    title: {
        fontSize: 16,
        fontWeight: 'bold',
        color: '#252A31',
    },
});
